name: charts
autocreate: true
image: io.sensiblemetrics/styled-java-patterns
command: ["bash"]
push:
  args:
    - URL=https://charts-$OKTETO_NAMESPACE.cloud.okteto.net
    - TASK=dev
sync:
- .:/usr/src/app
